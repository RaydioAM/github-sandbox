name: Add label to Salesforce Security RFI Issues

on:
  issues:
    types: opened

jobs:
  add_label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-ecosystem/action-add-labels@v1
        if: ${{ startsWith(github.event.issue.body, 'Test') }}
        with:
          labels: action-test-success
